/**
 * Helpers para construcción de URLs públicas
 * No expuestas directamente, pero usadas por los módulos públicos
 */
export declare function buildShareUrl(token: string, baseUrl: string): string;
export declare function buildImageUrl(token: string, baseUrl: string): string;
